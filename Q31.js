"use strict";
let userNames = ["Admin", "Blossom", "Bubbles", "Buttercup"];
// Check if the list of users is not empty
if (userNames.length === 0) {
    console.log("We need to find some users!");
}
else {
    userNames = [];
    console.log("All user have been removed!" + userNames.length);
}
