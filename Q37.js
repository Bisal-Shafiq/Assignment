"use strict";
// Define the function make_shirt
function makeShirt(size, message) {
    console.log(`The shirt size is ${size} and "${message}" `);
}
// Call the function with example arguments
makeShirt('Large', 'Hello World!');
makeShirt('Medium', 'TypeScript is awesome!');
makeShirt('Small', 'Keep Calm and Code On');
